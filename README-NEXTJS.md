# Forkeys - Next.js 15 版本

这是 Forkeys 密码管理器的 Next.js 15 迁移版本,保持了原有的所有功能和样式。

## 技术栈

- **框架**: Next.js 15.5+ (App Router)
- **语言**: TypeScript 5.7+
- **样式**: Tailwind CSS + 自定义 CSS 变量
- **加密**: CryptoJS (AES-256)
- **PWA**: Service Worker + Manifest
- **国际化**: 中文/英文

## 项目结构

```
forkeys/
├── public/                  # 静态资源
│   ├── icons/              # PWA 图标
│   ├── manifest.json       # PWA manifest
│   └── sw.js              # Service Worker
├── src/
│   ├── app/                # Next.js App Router
│   │   ├── api/           # API Routes
│   │   │   ├── register/
│   │   │   └── send_recovery_email/
│   │   ├── layout.tsx     # 根布局
│   │   └── page.tsx       # 主页面
│   ├── components/        # React 组件
│   │   ├── VaultProvider.tsx  # 全局状态管理
│   │   └── Modals.tsx     # 模态框组件
│   ├── hooks/             # React Hooks
│   │   ├── useVault.ts    # 密码库逻辑
│   │   ├── useTheme.ts    # 主题切换
│   │   └── useI18n.ts     # 国际化
│   ├── lib/               # 工具函数
│   │   ├── i18n.ts        # 国际化配置
│   │   └── utils.ts       # 工具函数
│   ├── styles/            # 样式文件
│   │   └── globals.css    # 全局样式
│   └── types/             # TypeScript 类型
│       └── index.ts
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.ts
```

## 开发命令

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 生产构建
npm run build

# 启动生产服务器
npm run start

# 代码检查
npm run lint
```

## 主要功能

### 已迁移功能 ✅

- ✅ 密码加密存储 (AES-256)
- ✅ 主密码验证
- ✅ 密码管理 (增删改查)
- ✅ 分类筛选
- ✅ 搜索功能
- ✅ 隐藏项目
- ✅ 密码生成器
- ✅ 主题切换 (亮色/暗色)
- ✅ 国际化 (中文/英文)
- ✅ 数据导入导出
- ✅ 密码恢复 (安全问题)
- ✅ 邮箱恢复
- ✅ 自动备份提醒
- ✅ PWA 支持

### 样式保持

所有原有的 CSS 样式、动画、响应式设计都已完整迁移,确保浏览效果与原版完全一致。

## 与原版的区别

### 优势

1. **现代化框架**: 使用 Next.js 15 App Router
2. **类型安全**: 完整的 TypeScript 支持
3. **组件化**: 模块化的 React 组件结构
4. **状态管理**: 使用 React Hooks 管理状态
5. **更好的开发体验**: 热更新、类型检查等
6. **SEO 友好**: Next.js 内置 SEO 优化
7. **性能优化**: 自动代码分割、优化加载

### API 说明

API Routes 已从 Python Flask 迁移到 Next.js API Routes:

- `POST /api/register` - 注册恢复信息
- `POST /api/send_recovery_email` - 发送恢复邮件

**注意**: 当前实现使用内存存储,生产环境需要配置数据库。

## 部署

### Vercel (推荐)

```bash
# 安装 Vercel CLI
npm i -g vercel

# 部署
vercel
```

### 自托管

```bash
# 构建
npm run build

# 启动
npm run start
```

服务器将在 http://localhost:3000 启动

### Docker

```bash
# 构建镜像
docker build -t forkeys .

# 运行容器
docker run -p 3000:3000 forkeys
```

## 环境变量

创建 `.env.local` 文件:

```env
# API Base URL (可选)
NEXT_PUBLIC_API_BASE_URL=https://your-api-domain.com

# 邮件服务配置 (如需邮件功能)
SMTP_SERVER=smtp.example.com
SMTP_PORT=465
SMTP_SENDER_EMAIL=your-email@example.com
SMTP_SENDER_PASSWORD=your-password
```

## 安全说明

1. **本地加密**: 所有密码数据在浏览器本地使用 AES-256 加密
2. **零知识**: 主密码从不上传服务器
3. **离线优先**: PWA 支持完全离线使用
4. **安全存储**: localStorage 加密存储

## 浏览器支持

- Chrome/Edge 90+
- Firefox 88+
- Safari 14+
- 移动浏览器 (iOS Safari, Chrome Mobile)

## 迁移说明

原始文件已备份为 `.bak` 扩展名:
- `index.html.bak`
- `service-worker.js.bak`
- `i18n.js.bak`

Python 后端 (`server.py`) 保留用于参考,但在 Next.js 版本中不再需要。

## 开发指南

### 添加新功能

1. 在 `src/types/index.ts` 添加类型定义
2. 在 `src/hooks/` 创建相应的 Hook
3. 在 `src/components/` 创建组件
4. 在 `src/app/page.tsx` 集成功能

### 修改样式

全局样式在 `src/styles/globals.css` 中,使用 CSS 变量支持主题切换。

### 添加国际化

在 `src/lib/i18n.ts` 的 `i18n` 对象中添加翻译文本。

## 许可证

与原项目保持一致

## 更新日志

### v2.0.0 (2025-01-XX)

- 🎉 完整迁移到 Next.js 15
- ✨ 使用 TypeScript 重写
- 🔧 组件化架构
- 📦 优化构建和部署
- 🎨 保持所有原有样式和功能
